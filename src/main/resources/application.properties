spring.application.name=fds_radar

#MySQL
spring.datasource.url=jdbc:mysql://fds-db.cbsk28cm8ilv.ap-northeast-2.rds.amazonaws.com:3306/fds_radar?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=admin
spring.datasource.password=cicd1234!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#Server
server.port=9090
server.servlet.session.timeout=30s

# 이상거래 탐지 AutoML 예측 서버(FastAPI) 주소
ai.server.url=http://localhost:8001

# AI 이상확률이 0.70 이상이면 FraudCase(조사 대상 사건)를 자동 생성, 미만이면 사건을 만들지 않음
fraud.threshold=0.70

# 사건 우선순위 산정 기준 — 0.90 이상 HIGH, 0.80 이상~0.90 미만 MEDIUM, 나머지(0.70 이상~0.80 미만)는 LOW로 자동 처리
fraud.priority.medium-min=0.80
fraud.priority.high-min=0.90

# JWT
jwt.secret=fds_radar_jwt_secret_key_2026_this_is_long_enough_for_hs256
jwt.expiration-ms=3600000

# 이상거래 탐지 AutoML 예측 서버(FastAPI) 주소
ai.server.url=http://localhost:8001

# AI 이상확률이 0.70 이상이면 FraudCase(조사 대상 사건)를 자동 생성, 미만이면 사건을 만들지 않음
fraud.threshold=0.70

# 사건 우선순위 산정 기준 — 0.90 이상 HIGH, 0.80 이상~0.90 미만 MEDIUM, 나머지(0.70 이상~0.80 미만)는 LOW로 자동 처리
fraud.priority.medium-min=0.80
fraud.priority.high-min=0.90